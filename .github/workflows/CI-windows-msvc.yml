---

name: Windows MSVC

on: [push, pull_request, workflow_dispatch]

jobs:
  windows_msvc:
    uses: ./.github/workflows/CI-launcher.yml
    with:
      min_runner: windows-2022
      min_toolchain_id: windows-msvc-194
      latest_runner: windows-2025-vs2026
      latest_toolchain_id: windows-msvc-195
